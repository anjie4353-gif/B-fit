/**
 * Bump when home-screen icon artwork changes (navy tile, logo, maskable).
 * Independent of APP_VERSION — forces icon refresh for already-installed users.
 */
export const ICON_PACK_VERSION = "2";